name: Megon CSS Minifier
on:
  push:
    branches:
      - main
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Combine and Minify CSS
        run: |
          mkdir -p dist/latest
          # ১. সব ফাইল জোড়া লাগানো (megon.css বাদ দিয়ে যেন লুপ না হয়)
          find src -name "*.css" -not -name "megon.css" -type f -exec cat {} + > dist/latest/megon.css
          
          # ২. মিনফাই করা এবং যেকোনো @import লাইন মুছে ফেলা
          grep -v "@import" dist/latest/megon.css | tr -d '\n' | sed 's/  */ /g' | sed 's/ :/:/g' | sed 's/ {/{/g' | sed 's/{ /{/g' | sed 's/ ;/;/g' | sed 's/; /;/g' > dist/latest/megon.min.css

      - name: Commit files
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -f dist/
          git commit -m "Auto-minify megon.css" || exit 0
          git push
