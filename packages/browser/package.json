{
  "name": "@megoncss/browser",
  "version": "1.0.0",
  "description": "MegonCSS browser/CDN build — JIT class scanning in the browser",
  "main": "./dist/megon.js",
  "browser": "./dist/megon.js",
  "scripts": {
    "build": "tsc && esbuild src/index.ts --bundle --minify --outfile=dist/megon.js",
    "dev": "esbuild src/index.ts --bundle --outfile=dist/megon.js --watch"
  },
  "files": [
    "dist/"
  ],
  "license": "MIT",
  "dependencies": {
    "@megoncss/core": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
